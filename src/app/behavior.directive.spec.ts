import { BehaviorDirective } from './behavior.directive';

describe('BehaviorDirective', () => {
  it('should create an instance', () => {
    const directive = new BehaviorDirective();
    expect(directive).toBeTruthy();
  });
});
